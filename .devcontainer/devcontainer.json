// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
	"name": "Terraform-EKS",
	// Specify the image used for the basis of the dev container
	// For private registries, connect using the VS Code Docker Extension
	// "image": "ironbank/pulumi-python:3.88.1-ubi",
	"build": {
		"dockerfile": "Dockerfile"
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"HashiCorp.terraform",
				"ms-azuretools.vscode-docker"
			]
		}
	},

	// Features to add to the dev container. More info: https://containers.dev/features.
	// "features": {},
	// "workspaceMount": "source=${localWorkspaceFolder},target=/opt/app-root/src/vscode,type=bind",
	// "workspaceFolder": "/opt/app-root/src/vscode",
	

	// "runArgs": ["--network=host","--init"],

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [],

	// Configure tool-specific properties.
	// "customizations": {},

	// Uncomment to connect as root instead. More info: https://aka.ms/dev-containers-non-root.
	"remoteUser": "root"
}
